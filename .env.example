# === Backend ===
LOG_LEVEL=INFO
# CORS: set to your frontend origin(s); leave empty to disable middleware
# Examples: ["http://localhost:5173","http://127.0.0.1:5173"]
ALLOW_ORIGINS=[]

# KEV refresh cadence (hours). KEV is always-on and cached if offline.
KEV_REFRESH_HOURS=6
# Optional: override feed (defaults to CISA official); normally leave unset
# KEV_FEED_URL=https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json

# Honeypot heuristics default (still toggleable in UI)
EXCLUDE_HONEYPOTS_DEFAULT=true
DEFAULT_MAX_SERVICES=45

# Prometheus metrics toggle
ENABLE_METRICS=true
LOG_LEVEL=INFO
ALLOW_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
DEFAULT_MAX_SERVICES=45
EXCLUDE_HONEYPOTS_DEFAULT=true
ENABLE_METRICS=true
KEV_REFRESH_HOURS=6
# KEV_FEED_URL=https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json
